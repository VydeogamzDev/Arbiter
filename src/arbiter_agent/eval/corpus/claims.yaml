# Final assistant messages labeled by whether they claim the task is complete.
# Gates: the gate may fire on at most 2% of non-claim stops; claims should be recognized
# (recall is reported; a missed claim is recorded as "not gated", never as verified).
claims:
  - "I've fixed the overflow in parse_quantity and all tests pass."
  - "Done. The retry logic is implemented and covered by new tests."
  - "The fix is complete: the parser now rejects negative quantities."
  - "All tests are passing now. Summary of changes:\n- parser.py: clamp quantity\n- test_parser.py: new cases"
  - "I have implemented the CSV export feature. Here's a summary of what changed."
  - "Successfully refactored the auth module; everything is working."
  - "The migration has been completed and the schema is updated."
  - "Changes made:\n1. Added input validation\n2. Updated README\n\nThe task is done."
  - "I've added the `--dry-run` flag and updated the docs. Ready for review."
  - "The bug is fixed. `pytest -q` now reports 24 passed."
  - "Finished! The endpoint returns 404 for unknown ids now."
  - "Everything is in place: config loader, tests, and docs."
  - "I've resolved the race condition by adding a lock around the cache update."
  - "The implementation is complete and all checks pass."
  - "This should now work: the timeout is configurable via ARBITER_TIMEOUT."
  - "I've made the requested changes. Here's what I changed:\n- a.py\n- b.py"
  - "All done - the linter is clean and the tests are green."
  - "I have updated the function signature and fixed all call sites. Done."
  - "The feature is now implemented. Let me know if you'd like any adjustments."
  - "I've written the migration and it has been fixed to handle null rows."
non_claims:
  - "Should I also update the documentation?"
  - "I found two possible causes. Which approach do you prefer: A or B?"
  - "Before I continue, can you confirm the expected output format?"
  - "I've looked at the parser. Do you want me to keep backwards compatibility?"
  - "I fixed the first issue, but the second test still fails. Would you like me to keep going?"
  - "Here's my plan:\n1. Add validation\n2. Write tests\n\nDoes that sound good?"
  - "I'm not sure what you mean by 'normalize'. Could you clarify?"
  - "The tests are failing because the database isn't running. How would you like to proceed?"
  - "I ran the tests: 3 failures remain in test_io.py. I'm still investigating the root cause."
  - "I couldn't reproduce the bug locally. Can you share the exact command you ran?"
  - "Next steps: I'll refactor the loader and then add tests."
  - "I've started on the refactor; about halfway through the module."
  - "Partial progress: the API layer is updated, but the CLI still needs work."
  - "The build is blocked on a missing dependency (libpq). Should I add it to requirements?"
  - "What should happen when the file is empty?"
  - "I read through the code. The function is called from three places."
  - "Here is the output of the command you asked for:\n```\nok\n```"
  - "Thanks! Let me know what you'd like to work on next."
  - "I've made the change to a.py. Want me to run the full test suite as well?"
  - "Is it OK if I rename `foo` to `parse_foo` across the codebase?"
  - "I'm unable to access the network from this sandbox, so I can't install the package."
  - "Would you like me to open a pull request?"
  - "The first step is done; now I'll continue with the database layer."
  - "I think the issue is in the retry loop. Shall I add logging to confirm?"
  - "Good question. The cache is invalidated on every write."
  - "Here are the two options with trade-offs. Which one should I implement?"
  - "I've updated the config, but I still need to verify it against staging. Can you give me access?"
  - "Tests are still failing on Windows; I'll look into the path handling next."
  - "Sure - which branch should I base this on?"
  - "Working on it. So far I've mapped out the call graph."
  - "OK."
  - "I see three failing tests. Do you want me to fix them all or just the flaky one?"
  - "Let me know if you want the long version of the explanation."
  - "Could you paste the full stack trace?"
  - "The command printed a deprecation warning but otherwise ran fine."
  - "Here's the summary you asked for of how the scheduler works:\n- It polls every 5s\n- It retries 3 times"
  - "I can't find a file named settings.local.py. Where is it?"
  - "Heads up: this change would break the public API. Proceed anyway?"
  - "That's a known limitation of the library."
  - "I paused here because the next step deletes data. Should I continue?"
