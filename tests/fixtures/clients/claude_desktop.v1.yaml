profile: claude_desktop
profile_version: 1
source: derived from the client's documented format (2026-09); replace with real recordings as clients are installed
entry_keys:
- command
- args
client_names:
- claude-ai
configs:
  typical: |
    {
      "mcpServers": {
        "filesystem": {
          "command": "npx",
          "args": [
            "-y",
            "fs"
          ]
        }
      },
      "globalShortcut": ""
    }
  empty: ''
